# 📒 Gerenciador de Contatos em Ruby

Projeto desenvolvido como **atividade prática da formação da Rocketseat**, com o objetivo de aplicar os conceitos básicos da linguagem **Ruby** e reforçar a lógica de programação por meio de um sistema simples de gerenciamento de contatos.

O programa permite ao usuário adicionar, listar e buscar contatos (nome e telefone), mantendo os dados salvos apenas enquanto a aplicação está em execução.

---

## 🚀 Funcionalidades

- Exibição de um menu interativo
- Adição de novos contatos (nome e telefone)
- Listagem de todos os contatos cadastrados
- Busca de um contato específico pelo nome
- Encerramento do programa pelo usuário

---

## 🧠 Conceitos praticados

- Entrada e saída de dados (`gets` e `puts`)
- Tipos de dados:
  - `String`
  - `Hash` (armazenamento dos contatos)
- Estruturas de controle:
  - Laço de repetição (`loop`)
  - Estrutura condicional (`case / when`)
- Validação básica de dados
- Lógica de programação

---

## ▶️ Como executar o projeto

1. Certifique-se de ter o **Ruby** instalado em seu computador  
2. Salve o arquivo com o nome `gerenciador_contatos.rb`
3. No terminal, execute o comando:

```bash
ruby gerenciador_contatos.rb
